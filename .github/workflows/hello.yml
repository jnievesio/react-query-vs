name: Procesos CICD
on:
  push:
    branches:
      - main
jobs:
  hello_world:
    #en que sistema operativo va a trabajar
    runs-on: ubuntu-latest
    steps:
      - name: Nombre del paso
        run: echo "Hello world"